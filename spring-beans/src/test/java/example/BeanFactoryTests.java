package example;

import example.domain.User;
import org.apache.catalina.core.ApplicationContext;
import org.junit.Assert;
import org.junit.Test;
import org.springframework.beans.factory.BeanFactory;
import org.springframework.beans.factory.xml.XmlBeanFactory;
import org.springframework.core.io.ClassPathResource;

/**
 * BeanFactory 测试案例
 *
 * @author <a href="mailto:ferenc.zeng@gmail.com">Ferenc Zeng</a>
 * @since 2019-07-25
 */
public class BeanFactoryTests {

	@Test
	public void testGetBeanFromBeanFactory() {
//		BeanFactory beanFactory = new XmlBeanFactory(new ClassPathResource("example/beanFactoryTest.xml"));
//		User user = (User)beanFactory.getBean("user2");
//		System.out.println("BeanNameAware id : " + user.getId());
//		Assert.assertEquals("ference", user.getUsername());

//		ApplicationContext context = new ClassPathXml(new ClassPathResource("example/beanFactoryTest.xml"));
//		User user = (User)beanFactory.getBean("user2");
//		System.out.println("BeanNameAware id : " + user.getId());
//		Assert.assertEquals("ference", user.getUsername());
	}





}
